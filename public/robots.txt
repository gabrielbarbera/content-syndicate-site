# robots.txt - Production Domain Configuration
# For production domain: contentsyndicate.net

# Allow all crawlers on production
User-agent: *
Allow: /

# Disallow specific paths if needed
# Disallow: /admin/
# Disallow: /api/

# Sitemap location
Sitemap: https://contentsyndicate.net/sitemap.xml

# Crawl delay (optional)
# Crawl-delay: 1

# Note: Preview domains (*.vercel.app) are blocked via:
# - X-Robots-Tag HTTP header (see vercel.json)
# - Meta robots tag injected via inline script (see index.html)
# - React component backup (see RoboMetaTag.tsx)
